'use strict';

/*global describe, it, jest, expect*/
jest.dontMock('../index');

describe('Dropzone',  function () {
    it('changes the state after click', function () {
        var React = require('react/addons');
        // var TestUtils = React.addons.TestUtils;
        // var Dropzone = require('../index');

        expect(1).toBe(1);
    });
});
